import { Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function FavoriteScreen() {
  return (
    <SafeAreaView>
      <Text>Favoritos perros</Text>
    </SafeAreaView>
  )
}